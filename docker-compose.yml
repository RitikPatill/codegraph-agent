version: "3.9"

services:
  backend:
    # TODO: add Dockerfile at backend/Dockerfile
    build: ./backend
    ports:
      - "8000:8000"
    # TODO: add environment variables, volumes for KG persistence

  frontend:
    # TODO: add Dockerfile at frontend/Dockerfile
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend

# Note: images not yet published — docker compose up will fail until
# Dockerfiles are added in a later milestone.
