repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.10
    hooks:
      # Format first so the linter sees already-formatted code
      - id: ruff-format
        files: ^backend/.*\.py$
      - id: ruff
        args: [--fix]
        files: ^backend/.*\.py$

  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v3.4.2
    hooks:
      - id: prettier
        files: ^frontend/.*\.(ts|tsx|json|css)$
